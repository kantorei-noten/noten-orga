# Vorlage — als .env kopieren (die .env ist gitignored, kommt NIE ins Repo).
# Werte für die lokale Entwicklung:
NOTEN_DATABASE_URL=postgresql://noten_app:devpw@localhost:5433/noten
NOTEN_ENVIRONMENT=dev
NOTEN_SESSION_SECRET=bitte-fuer-den-betrieb-aendern
NOTEN_LOG_LEVEL=INFO
